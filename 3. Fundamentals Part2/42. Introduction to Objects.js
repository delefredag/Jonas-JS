const multipleLineArray = [
    'Ji',
    'Kim',
    2022-1994,
    'Student',
    ['array', 'inside', 'array2']
];
console.log(multipleLineArray);

const object = { } //actual object 

//Object Literal Syntax 
const ji = {  //each key has a value. each of these keys is also called a property.
    firstName : 'Ji',   //colon og comma 
    lastName : 'Kim',  //property lastName with the value of Kim
    age : 2023- 1994,
    job : 'Student',
    learning : ['array', 'inside', 'array2']
}

// ji안에 다 들어가있는 프로퍼티들 
// 객체와 배열의 차이점 
// -> the order of this doesn't matter at all when we wanna retrieve them.💡
// 배열 : the order in which we specify the elements matters a lot!! that's how we access these elements 😉
// 객체 : more for unstructured data, 이름이 필요하면 객체로 